"""Copy this adapter into an NLP candidate folder and replace the stub."""


def build_agent(catalog_path: str):
    """Return the candidate Agent implementing reset/respond."""

    raise NotImplementedError("wire the NLP candidate to the shared Agent contract")
