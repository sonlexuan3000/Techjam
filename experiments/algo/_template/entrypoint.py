"""Copy this adapter into an algorithm candidate folder and replace the stub."""


def build_agent(catalog_path: str):
    """Return the candidate Agent implementing reset/respond."""

    raise NotImplementedError("wire the algorithm candidate to the shared Agent contract")
